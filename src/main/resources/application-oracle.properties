# Oracle数据库专用配置
app.datasource.type=oracle

# 禁用Hibernate DDL自动更新，避免连接失败时应用启动失败
spring.jpa.hibernate.ddl-auto=none

# 使用Oracle方言
spring.jpa.database-platform=org.hibernate.dialect.OracleDialect

# 其他配置继承主配置文件
